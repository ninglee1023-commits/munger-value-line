# 芒格原报告同步与发布

## 原则

「芒格式公司分析」负责产出研究；本网站接收已经完成的完整原报告并发布。使用原报告全文作为唯一正文来源，不重新运行类似提示词，不根据任务摘要、检索摘要或记忆重建研究，也不删改原报告以凑成固定章节数量。只能拿到摘要、截断文字或失效附件时，保留待同步状态并取得完整来源，不能声称已同步。

已有结构化 JSON 报告保留。新同步使用 Markdown 格式，文章目录依据原文标题生成；网站摘要、卡片和图表是展示补充，不能代替或覆盖原文。图表只使用原报告已经核对的数据，无数据时可省略，不能为凑四图编造数字。卡片元数据来自原报告；不要添加新的投资结论。

## 私有交接与去重

1. 在网站仓库之外保存完整来源消息或已下载附件，以及原始 UTF-8 Markdown。保留标题、原研究日期、来源任务和消息标识、取得时间及附件版本，供追溯。
2. 用完整消息标识定位一个来源版本；对原始 Markdown 字符串按 UTF-8 计算 SHA-256，包含原有换行、标点和空白。不得在计算后改换行、转繁简或润色正文。网页 `markdown` 和生成 PDF 的输入都必须使用该字符串。
3. 私有去重台账以来源任务＋消息标识和 SHA-256 记录接收、验证、待发布及已发布状态。同一来源、同一哈希重复到达时跳过；同一来源内容变化时记录为新修订，保留上一版本证据。不要仅凭公司名称或同步日期判定重复。
4. 公共来源元数据只保存来源标题、原研究日期、同步时间、哈希及可选的公开图片目录 `assetBase`。禁止把私有任务／消息标识、私有对话 URL、用户资料或访问凭据放入 JSON、README、Git 提交信息和发布目录。外部公开来源链接仍保留在原报告及来源账本。
5. `research-history.json` 保留原研究日和修订／同步记录。同步日期不能冒充研究日期，也不能重置 30 天选题去重窗口。

## 公开报告字段

以下为结构示意；正文及哈希必须来自真实完整原报告，不能发布占位文字。

```json
{
  "slug": "company-id",
  "company": "公司名称",
  "ticker": "股票代码",
  "format": "markdown",
  "markdown": "完整原报告 Markdown 字符串",
  "asof": "2026-09-18",
  "source": {
    "title": "芒格式公司分析 · 公司名称",
    "researchDate": "2026-09-18",
    "syncedAt": "2026-09-19T10:30:00+08:00",
    "contentSha256": "原始 Markdown 的 64 位小写 SHA-256"
  },
  "summary": ["从原报告提取的原有结论"],
  "basis": "原报告的日期、期间与会计口径说明",
  "sources": [
    {"id": "S1", "title": "公开原始资料标题", "url": "https://example.com/source", "detail": "来源说明"}
  ],
  "pdf": {"url": "reports/company-id-20260918.pdf", "label": "下载同版 PDF"}
}
```

`source` 的四个基本字段用于原文同步；有原图时可另加 `assetBase: "reports/company-date/"`，用于解析原稿中的 `charts/figure.png` 相对路径，Markdown 字符串保持不变。新增同步报告必须同时提供已检查的同版中文 PDF；`pdf` 字段可省略仅用于旧稿兼容。PDF 链接可使用 HTTPS 或网站内安全的相对 `.pdf` 路径，不使用本机路径、私有对话链接或失效 sandbox 链接。内部相对路径和文件名使用英文字母、数字、短横线或下划线。同步时间使用含时区的 ISO 8601 时间；原研究日来自源报告。

`app.js` 的 `CATALOG` 登记 `id/file/name/ticker/date/sector/verdict/valuation/conclusion/lead/metrics/visuals`。`date` 使用原研究日；`visuals` 和 `metrics` 可为空数组。正文不限 14 或 17 章，目录不截断原有标题。优先提取一级、二级标题作为导航，三级标题保留在正文。

## 同一原文生成 PDF

PDF 与网页都使用已核对哈希的同一份 Markdown。使用支持中文的字体并嵌入或子集嵌入，不能使用不含中文字形的默认西文字体。对旧 PDF 的乱码修复也应从完整原稿重新生成，不根据乱码猜写报告。

如果唯一完整来源是 PDF，可从已读出的全部页面转录为 Markdown，修复标题、段落和表格格式；必须核对文字、数字、正负号、表格单元格和顺序守恒，并清楚标为「原 PDF 全文转录」，不能称为取得原始 Markdown。图表仅从原表重画，来源网址根据原来源名称核实。版面及字体修复不等于重写研究，不借机刷新价格、更新财务数据或修改投资判断。将核对后的转录稿作为网页与修复 PDF 的同一输入，另外保留原 PDF／提取文字及转录 QA 证据。

仓库的 PDF 工具为 `tools/pdf/markdown_to_pdf.py` 和其 Markdown 解析助手 `tools/pdf/markdown_tokens.cjs`。运行方式：

```text
python tools/pdf/markdown_to_pdf.py <final.md> <output.pdf> --qa-dir <private-qa-folder>
```

QA 目录须位于发布目录之外；输入路径也通过运行参数传入，不写入公共资料。保持 Markdown 与相对图片目录的结构。该命令生成与检查产物之后，还须完成下述逐页视觉核验，才可将 PDF 放入网站。

交付前检查：PDF 实際打开成功、逐页渲染后中文和表格没有缺字／方框／乱码、文字提取可读、来源链接可点开、页边没有裁切。核对标题、研究日、章节顺序、表格数值与原稿一致。PDF 文件存在并通过这些检查之后，才在公共 JSON 加 `pdf`。网站的 `qa.cjs` 会检查本地 PDF 文件头及路径和网页入口，但不能替代逐页 PDF 检查。

## 验证与发布

1. 接收原文，完成来源标识和哈希去重，生成报告 JSON及经检查的同版中文 PDF，登记目录与研究历史。
2. 执行 `node --check app.js` 和 `node qa.cjs`，检查桌面与手机的全文、目录、来源、表格、可选图表与 PDF 入口。QA 根据实际数量检查，不强制统一章节数或版本字符串。
3. 检查差异：现有研究内容没有被同步流程改写，全部新公开文件不含私有任务／消息标识，JSON 中原文哈希与私有原稿一致。
4. GitHub Pages 继续从 `main` 分支根目录发布，没有 `dist` 副本。只提交本次已验证的文件；更新脚本／样式时同步更新 `index.html` 的资源版本，避免缓存旧版。
5. 按已有用户授权提交和推送后，等待 Pages 部署成功，再运行 `node qa.cjs https://ninglee1023-commits.github.io/munger-value-line/`。确认实际线上 JSON 与本地版本相同、完整正文和 PDF 链接可用，最后才记为已发布。
6. 无新原报告则不重复生成或重复发布。源报告不完整、权限或部署失败时，保留可恢复的待同步记录，明确报告原因；不把本地准备完成说成网站已上线。

`rebuild.cjs` 是 2026-09-18 的一次性迁移工具，会从固定旧基线重建，不能用于日常原稿同步。

## 攜程 2026-09-19 首篇同步

`tools/sync-tripcom.cjs` 接收最终原稿和已通过检查的 PDF，更新攜程一篇及目录／研究历史，其他 12 篇数据文件保持不变。原稿中的三幅图分别为收入、毛利率与营业率、GAAP／正常化盈利与 FCF 比较；第三幅不是可加总的财务桥。脚本从原表取数，保留 2011–2025 全部十五年，2016 FCF 的 N/A 不补零。

```text
node tools/sync-tripcom.cjs --markdown <final.md> --pdf <checked.pdf> --dry-run
node tools/sync-tripcom.cjs --markdown <final.md> --pdf <checked.pdf>
```

脚本保存精确原文、三幅图片和 PDF，更新 `index.html` 资源版本；相同哈希重跑会复用同步时间并跳过重复历史记录，不提交或推送。攜程卡片标示「芒格原稿」，既有报告保留「完整研究」，不会把旧研究冒称同步原稿。以后新公司应使用同一交接规范，不直接复用攜程专用的财务行校验。
